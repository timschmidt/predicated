[package]
name = "hyperlimit"
version = "0.4.1"
edition = "2024"
description = "Hyperreal-backed exact predicates with structural Real awareness."
license = "MIT OR Apache-2.0"
authors = ["Timothy Schmidt <timschmidt@gmail.com>"]
repository = "https://github.com/timschmidt/hyperlimit"
readme = "README.md"
keywords = ["exact", "geometric", "predicates", "boundary", "orientation"]
categories = ["mathematics", "games", "simulation"]

[dependencies]
hyperreal = { version = "0.13.1", path = "../hyperreal" }
hyperlattice = { version = "0.6.1", path = "../hyperlattice" }
rayon = { version = "1.10", optional = true }

[dev-dependencies]
criterion = "0.5"
proptest = "1.7.0"
robust = "1.2.0"
serde_json = "1"

[features]
default = ["std"]
std = []
dispatch-trace = ["hyperreal/dispatch-trace"]
parallel = ["std", "dep:rayon"]
serde = ["hyperreal/serde", "hyperlattice/serde"]

[[example]]
name = "readme_quickstart"

[[bench]]
name = "predicates"
harness = false
